# Copy to .env and fill values
SECRET_KEY=replace_me_with_random_secret
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
OPENAI_API_KEY=
# Optional: enable embedding-based retrieval
USE_OPENAI_EMBEDDINGS=false
# SMTP (email) settings - required to send verification codes
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
